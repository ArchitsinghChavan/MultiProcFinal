Run `g++ test_cur_brtree.cpp -o test_cur_brtree -fopenmp; clear; ./test_cur_brtree` to test the concurrent tree

Run `g++ test_seq_brtree.cpp -o test_seq_brtree -fopenmp; clear; ./test_cur_brtree` to test the sequential tree